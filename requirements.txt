# Core Data Science Libraries
pandas>=1.5.0
numpy>=1.24.0
scikit-learn==1.2.2  # Downgraded to a stable version to avoid Cython issues

# Machine Learning Models
xgboost>=1.7.0
imbalanced-learn>=0.10.1  # Downgraded to ensure compatibility with scikit-learn 1.2.2

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0
plotly>=5.15.0

# Web Frameworks
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
streamlit>=1.28.0
requests>=2.31.0

# Model Persistence
joblib>=1.2.0  # Downgraded to match scikit-learn 1.2.2

# Data Processing (if CSV files are used)
openpyxl>=3.1.0

# System utilities (already included with Python)
# os, sys, warnings, pickle, time are built-in modules
waitress>=2.1.0

# Build Tools (required for compiling scikit-learn)
Cython>=0.29.28,<3.0.0  # Compatible with scikit-learn 1.2.2